"""
The pdart.new_db package contains code to build SQLite databases for
PDART archives.

**New to PDART?** Look in :mod:`pdart.new_db.CreateDatabase` to see the
SQL used to construct the SQLite database tables.  If you need to
rebuild the database, use
:func:`pdart.new_db.CreateDatabase.create_database`.
"""
